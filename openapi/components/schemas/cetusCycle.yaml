allOf:
  - $ref: ./worldstateObject.yaml
  - required:
      - isCetus
      - isDay
      - timeLeft
    type: object
    properties:
      isDay:
        type: boolean
      state:
        type: string
        description: Describes the current time. e.g. "day" or "night"
      timeLeft:
        minLength: 1
        type: string
      isCetus:
        type: boolean
