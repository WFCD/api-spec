type: object
properties:
  mission:
    $ref: ./mission.yaml
  active:
    type: boolean
  expired:
    type: boolean
  eta:
    minLength: 1
    type: string
  id:
    minLength: 1
    type: string
  expiry:
    minLength: 1
    type: string
    format: date-time
  activation:
    minLength: 1
    type: string
    format: date-time
  rewardTypes:
    type: array
    items:
      $ref: ./rewardType.yaml
required:
  - activation
  - active
  - eta
  - expired
  - expiry
  - id
