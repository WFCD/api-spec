allOf:
  - $ref: ./worldstateObject.yaml
  - type: object
    properties:
      params:
        type: object
        properties: { }
      rewardTypes:
        type: array
        items:
          type: string
      season:
        type: number
      tag:
        type: string
      phase:
        type: number
      possibleChallenges:
        type: array
        items:
          $ref: ./nightwaveChallenge.yaml
      activeChallenges:
        type: array
        items:
          $ref: ./nightwaveChallenge.yaml
