type: object
properties:
  rivenCompatability:
    type: object
    description: The name of the weapon that this riven is compatible with
    additionalProperties:
      type: object
      properties:
        rolled:
          $ref: ./rivenStatistic.yaml
        unrolled:
          $ref: ./rivenStatistic.yaml
        rerolled:
          $ref: ./rivenStatistic.yaml
    example:
      Corvas:
        rerolled:
          itemType: Archgun Riven Mod
          compatibility: Corvas
          rerolled: true
          avg: 291.94
          stddev: 346.96
          min: 25
          max: 1500
          pop: 0.0536
          median: 150
