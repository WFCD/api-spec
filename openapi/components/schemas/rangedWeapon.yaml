allOf:
  - $ref: ./item.yaml
  - $ref: ./weapon.yaml
  - type: object
    properties:
      accuracy:
        type: number
      ammo:
        type: number
      fireRate:
        type: number
        format: float
      magazineSize:
        type: integer
      multishot:
        type: number
        format: float
      noise:
        type: string
      trigger:
        type: string
