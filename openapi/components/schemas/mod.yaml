allOf:
  - $ref: ./item.yaml
  - type: object
    properties:
      baseDrain:
        type: integer
      compatName:
        type: string
      fusionLimit:
        type: integer
      levelStats:
        type: array
        items:
          type: object
          properties:
            stats:
              type: array
              items:
                type: string
      polarity:
        $ref: ./polarity.yaml
      transmutable:
        type: boolean
