allOf:
  - $ref: '#/components/schemas/shallowItem'
  - type: object
    properties:
      patchlogs:
        type: array
        items:
          $ref: '#/components/schemas/patchlog'
      components:
        type: array
        items:
          $ref: '#/components/schemas/shallowItem'
      introduced:
        $ref: '#/components/schemas/introduced'
      estimatedVaultDate:
        type: string
        format: date
