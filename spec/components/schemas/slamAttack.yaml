type: object
properties:
  damage:
    type: number
    format: float
  radial:
    type: object
    properties:
      damage:
        type: number
        format: float
      element:
        $ref: '#/components/schemas/element'
      proc:
        type: number
        format: float
      radius:
        type: number
        format: float
