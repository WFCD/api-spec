allOf:
  - $ref: '#/components/schemas/item'
  - type: object
    properties:
      url:
        type: string
        format: url
      mr:
        type: number
      riven_disposition:
        type: number
      polarities:
        type: array
        items:
          $ref: '#/components/schemas/polarity'
      thumbnail:
        type: string
        format: url
      tags:
        type: array
        items:
          type: string
      vaulted:
        type: boolean
      introduced:
        $ref: '#/components/schemas/introduced'
      marketCost:
        type: string
      bpCost:
        type: string
      attacks:
        type: array
        items:
          $ref: '#/components/schemas/attack'
      masteryReq:
        type: integer
      buildPrice:
        type: integer
      buildTime:
        type: integer
      skipBuildTimePrice:
        type: integer
      buildQuantity:
        type: integer
      consumeOnBuild:
        type: boolean
      wikiaThumbnail:
        type: string
        format: url
      wikiaUrl:
        type: string
        format: url
      criticalChance:
        type: number
        format: float
      criticalMultiplier:
        type: number
        format: float
      disposition:
        type: integer
      fireRate:
        type: number
        format: float
      omegaAttenuation:
        type: number
        format: float
      procChance:
        type: number
        format: float
      releaseDate:
        type: number
        format: float
      slot:
        type: integer
        description: Specifies the slot of an item.
      totalDamage:
        type: integer
      vaultDate:
        type: string
        format: date
