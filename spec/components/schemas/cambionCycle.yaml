required:
  - expiry
  - activation
  - id
  - active
type: object
properties:
  id:
    minLength: 1
    type: string
  expiry:
    $ref: '#/components/schemas/timestamp'
  activation:
    $ref: '#/components/schemas/timestamp'
  active:
    type: string
    enum:
      - vome
      - fass
  timeLeft:
    minLength: 1
    type: string
