get:
  tags:
    - Catch All
  parameters:
    - in: path
      name: star
      schema:
        type: string
        enum:
          - '*'
      required: true
      description: Describes a catch-all scenario
  summary: Catch all
  responses:
    '404':
      $ref: '#/components/responses/NotFound'
