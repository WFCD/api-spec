get:
  tags:
    - worldstate
    - unstable
  summary: '[Unstable] Current Kuva Mission listing'
  description: Current Kuva Mission listing (provided by semlar).
  parameters:
    - $ref: '#/components/parameters/platform'
  responses:
    '200':
      description: successful operation
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/kuva'
